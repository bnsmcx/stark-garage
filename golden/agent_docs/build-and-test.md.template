# Build & Test Commands

> This file is generated by `/bootstrap` for your specific project.
> Template placeholders are replaced with detected commands.

## Commands

| Command | Purpose |
|---------|---------|
| `{{validation_command}}` | **Validation** — hard gate for all workflow commands |
| `{{build_command}}` | Build the project |
| `{{test_command}}` | Run tests |
| `{{lint_command}}` | Run linter |
| `{{run_command}}` | Run locally (dev mode) |

## Docker / Services

{{docker_services}}

## Notes

{{build_notes}}
